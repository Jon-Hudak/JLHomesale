---
interface Props {
  as?: any; // Allows passing 'li', 'article', 'div', etc.
  class?: string;
}

const { as: Tag = "div", class: className = "" } = Astro.props;
---

<Tag class={`rounded-lg border border-neutral-300 bg-white p-8 shadow-sm ${className}`}>
  <slot />
</Tag>